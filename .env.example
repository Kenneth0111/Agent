# Copy to .env for local configuration. Never commit real secrets.
# Spring Boot reads process environment variables, not this file automatically.
SERVER_ADDRESS=127.0.0.1
SERVER_PORT=18080

# Used in later model-integration tasks; currently no paid API calls are made.
DEEPSEEK_API_KEY=
